{% extends '../content_base.html.twig' %}

{% block content_body %}
    <h1> Edit this book</h1>

    {{ form_start(bookForm) }}
    {{ form_row(bookForm.imageFile) }}
        {{ form_widget(bookForm) }}

        <button type = "submit" class = "btn btn-primary">Update</button>
    {{ form_end(bookForm) }}
{% endblock %}