<header class="navbar navbar-expand-sm bg-dark navbar-dark">
    <a class="btn btn-primary" href="{{ path('index') }}">
        Library
    </a>
    <nav class="collapse navbar-collapse" id="nav-items">
        <ul class="navbar-nav mr-auto">
            {{ render_esi(url('showGenre')) }}
        </ul>
        <a class="btn btn-info" href="{{ path('new') }}">
            Add new book
        </a>
        <a class="btn btn-info" href="{{ path('newauthor') }}">
            Add new author
        </a>
        <a class="btn btn-info" href="{{ path('newgenre') }}">
            Add new genre
        </a>
        <a class="btn btn-primary" href="{{ path('bookList') }}">
            Edit your books
        </a>
        {#TODO#}
{#        <form class="form-inline my-2 my-md-0" action="" method="get">#}
{#            <input class="form-control search" type="text" placeholder="Search Book">#}
{#        </form>#}
    </nav>
</header>
